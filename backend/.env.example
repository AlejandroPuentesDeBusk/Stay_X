
# ==============================================================================
# CONFIGURACIÓN GENERAL DEL SERVIDOR (StayX)
# ==============================================================================
PORT=7001
NODE_ENV=development

# CORS Origins (separados por coma, ej: http://localhost:3000,https://app.stayx.com)
CORS_ORIGINS=http://localhost:7000,http://localhost:4321

# Rate Limiting
RATE_LIMIT_WINDOW_MS=60000
RATE_LIMIT_MAX=100
METRICS_PUBLIC=true

# URL base del frontend (CRÍTICO para correos y OAuth)
FRONTEND_URL=http://localhost:7000


# ==============================================================================
# CONFIGURACIÓN DE BASES DE DATOS
# ==============================================================================

# PostgreSQL (Principal - Datos Relacionales)
# Ejemplo: postgres://user:pass@host:5432/dbname
DATABASE_URL=postgres://stayx_user:mysecretpass@localhost:5432/stayx_db

# Redis (Cache y Pub/Sub para Chat)
REDIS_URL=redis://localhost:6379

# Opciones de sincronización de Sequelize: off | alter | force
DB_SYNC=alter 


# ==============================================================================
# CONFIGURACIÓN DE AUTENTICACIÓN (JWT)
# ==============================================================================
# Secreto de al menos 16 caracteres de longitud
JWT_SECRET=reemplaza_con_un_secreto_largo_y_seguro
JWT_ISSUER=StayX
JWT_AUDIENCE=stayx-app


# ==============================================================================
# GOOGLE OAUTH
# ==============================================================================
GOOGLE_CLIENT_ID=TU_CLIENT_ID_DE_GOOGLE
GOOGLE_CLIENT_SECRET=TU_CLIENT_SECRET_DE_GOOGLE
GOOGLE_REDIRECT_URI=http://localhost:7001/api/auth/google/callback


# ==============================================================================
# PROVEEDORES DE PAGO (Stripe)
# ==============================================================================
# Llave secreta de Stripe
STRIPE_SECRET_KEY=
# Secreto de webhook para verificar las firmas entrantes
STRIPE_WEBHOOK_SECRET=
# Prefijo de planes de búsqueda (ej: stayx_starter, stayx_pro)
STRIPE_PRICE_LOOKUP_PREFIX=stayx


# ==============================================================================
# MAILING (Nodemailer)
# ==============================================================================
MAIL_FROM="StayX <no-reply@stayx.com>"
# Usa ETHEREAL=true en desarrollo si no tienes un SMTP real configurado
# ETHEREAL=true
SMTP_HOST=smtp.ejemplo.com
SMTP_PORT=587
SMTP_USER=usuario_smtp
SMTP_PASS=contrasena_smtp


# ==============================================================================
# ALMACENAMIENTO DE ARCHIVOS (Google Cloud Storage)
# ==============================================================================
# CLAVE: La ruta al archivo JSON de credenciales de GCS (si no usas IAM de Cloud Run/GKE)
# GOOGLE_APPLICATION_CREDENTIALS=/path/to/your/gcs-key.json
GCS_BUCKET_NAME=stayx-prod-assets
```